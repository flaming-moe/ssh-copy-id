# Windows replacement for ssh-copy-id

This is using existing ssh command to perform remote exec to enter a public key to the authorized_keys file.

## What is missing

Validation if the public key is valid
